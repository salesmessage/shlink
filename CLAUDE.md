# Claude Code entry point

Claude Code auto-loads `CLAUDE.md`, not `AGENTS.md`. This file exists only to pull in `AGENTS.md`, which stays the single source of instructions for every agent.

@AGENTS.md
